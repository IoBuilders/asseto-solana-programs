[package]
name = "cmtat-transfer"
version = "0.1.0"
description = "Created with Anchor"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "cmtat_transfer"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
anchor-debug = []
custom-heap = []
custom-panic = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]

[dependencies]
anchor-lang = { version = "0.32.1", features = ["interface-instructions"] }
anchor-spl = "0.32.1"
spl-token-2022 = { version = "=8.0.1", features = ["no-entrypoint"] }
cmtat-freeze = { path = "../cmtat-freeze", features = ["cpi"] }
cmtat-common = { path = "../cmtat-common" }
cmtat-transfer-control = { path = "../cmtat-transfer-control", features = ["cpi"] }
cmtat-transfer-hook = { path = "../cmtat-transfer-hook", features = ["cpi"] }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("solana"))'] }
