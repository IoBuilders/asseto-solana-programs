[package]
name = "snapshot"
version = "0.1.0"
description = "Created with Anchor"
edition = "2021"
license.workspace = true

[lib]
crate-type = ["cdylib", "lib"]
name = "snapshot"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
anchor-debug = []
custom-heap = []
custom-panic = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build", "common/idl-build"]

[dependencies]
anchor-lang = { version = "1.0.2", features = ["init-if-needed", "event-cpi"] }
anchor-spl = "1.0.2"
spl-token-2022 = { version = ">=10.0.0, <11", features = ["no-entrypoint"] }
common = { path = "../common" }
solana-system-interface = ">=2.0.0"

[lints.rust]
unexpected_cfgs = { level = "allow" }
